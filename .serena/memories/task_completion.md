# Task completion
- For normal plugin changes, run `npm test` and `npm run build` before claiming completion.
- If updating bundled output for local Obsidian use, copy `manifest.json`, `main.js`, and `styles.css` to `/Users/Zipper/Obsidian/ObsidianTest/.obsidian/plugins/obsidian-webdav-sync` and verify with `cmp`.
- Do not commit unless explicitly requested.