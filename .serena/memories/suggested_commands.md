# Suggested commands
- Install dependencies: `npm install`
- Run tests: `npm test`
- Build plugin bundle: `npm run build`
- Development watch build: `npm run dev`
- Common inspection: `git status --short --branch`, `rg <pattern>`, `rg --files`
- Release artifacts are `manifest.json`, `main.js`, and `styles.css`.